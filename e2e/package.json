{
  "name": "whatsapp-clone-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "End-to-end feature tests for the chat app",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.0"
  }
}
